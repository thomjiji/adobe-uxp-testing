{
  "name": "uxp-test-2-typescript",
  "version": "1.0.0",
  "description": "TypeScript testing environment for Adobe UXP Premiere Pro APIs",
  "author": "Adobe Inc",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc && mkdir -p dist && cp src/index.html src/style.css manifest.json dist/ && ([ -d icons ] && cp -r icons dist/ || true)",
    "watch": "tsc --watch",
    "clean": "rm -rf build-html",
    "format": "prettier --write \"**/*.{ts,js,json,html,css}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json,html,css}\""
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
